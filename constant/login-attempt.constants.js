const MAX_LOGIN_ATTEMPTS = 5;
const LOGIN_LOCKOUT_DURATION_MINUTES = 30;
const MIN_LOGIN_ATTEMPTS_WARNING = 2

module.exports = { MAX_LOGIN_ATTEMPTS, LOGIN_LOCKOUT_DURATION_MINUTES, MIN_LOGIN_ATTEMPTS_WARNING };
